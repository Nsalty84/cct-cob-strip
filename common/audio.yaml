media_player:
  - platform: i2s_audio
    name: ${friendly_name} media player
    dac_type: external
    i2s_dout_pin: 32
    mode: mono

microphone:
  - platform: i2s_audio
    id: external_mic
    adc_type: external
    i2s_din_pin: 26
    pdm: false
